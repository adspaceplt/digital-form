# ADspace Digital Portal — working rules

@DESIGN.md
@STANDARD.md

Static site, vanilla JS, no build step, served by GitHub Pages at
digital.adspace.me (CNAME in the repo). Pages: `admin/` (console),
`creators/` (client-facing creator selection), `review/` (client-facing
content review). Supabase behind `js/api.js`; schema in
`supabase/schema.sql` (re-runnable; the user runs it by hand in the SQL
editor, and must be told when). A change confined to one function or one
column ships instead as a dated file in `supabase/migrations/`, narrowly
scoped and safe to run twice, so a live database is not put through fifteen
unrelated data migrations to replace one function body.

The design system, the components, the laws and the copy rules are in
`DESIGN.md`. The general engineering and design standard, the working method
and the completion gate are in `STANDARD.md`, whose Project Memory records the
three places this portal deliberately departs from it (the spacing scale, the
14px console body, and the 1280/390 viewport matrix). Both are imported above. Read all
three before any change; where they disagree, `STANDARD.md` §2 settles it and
this portal's documented behaviour wins.

## 1. Verified code engine

### Locked features (merged to `main`, PRs #3 to #52)

| Area | Locked behaviour |
|---|---|
| Theme (`css/portal.css`, `js/admin.js`) | Light is the default everywhere and never taken from `prefers-color-scheme`. Dark is the console's alone, chosen from a toggle in the sidebar foot above Sign out, kept in `localStorage` as `adspace-theme`, applied by a head script before first paint, and hung off `:root[data-theme="dark"]`. `/client/`, `/creators/` and `/review/` never carry the attribute, so a client never decides on a proposal in a register nobody chose. |
| Screen typeface | System stack for everything except the display sizes, which take Slate Regular through `--font-head` (`/css/SlateRg.woff2`, 38KB, `font-display: swap`; the TTF stays for pdf-lib and as the fallback). Optima is the letterhead wordmark and stays out of the portal. |
| Header and footer (`js/chrome.js`) | One chrome on every page: mark from `brandLogo`, kicker from `data-kicker`, actions from `data-actions` (`lang`, `qr`); "Digital Portal" and "Logged in as {email}" in the console; outlined Sign out with icon. Footer left `© {year} ADSPACE PLT. All Rights Reserved.`, right `Terms of Service` → https://adspacestudios.com/legal/policies; footer on the floor on short pages. |
| Full-page states | One `.cover` component for no link, bad link, access code, closed, confirmed, nothing to review, access denied; same words on `creators/` and `review/`. |
| Content Review (`review/`, `js/review.js`, `js/mockups.js`) | Post mockups per platform (Instagram feed 4:5 1080×1350, Reels and Stories 9:16 at cover ratio, TikTok, Facebook feed and carousel, rednote); Facebook carousel: 2 tiles side by side, 4 as 2×2, 5 as 2 above 3 with `+N` greyed on the fifth; handle shown on the mockup, not the client name; client logo fills the circle with no border or shadow; placeholder avatar is an illuminance gradient, never grey or rainbow; Approve needs a name and Approved reads outlined with Request changes hidden; Copywriting label, copy text control at the top; the Instagram send glyph is the one concave dart Instagram draws, not Feather's `send` (a quadrilateral plus a separate crease from the same apex: at 22px with a round cap the two strokes pile up at that corner and the mark reads as a smudge), because a mockup is only a preview while every mark in it is the one the client's audience sees; **the caption block is spaced the way the app spaces it**: Save hangs off the far right of the action row (`.mk-act-save`, `margin-left: auto`) and never sits against Share, the `more` toggle rides the end of the truncated line over a short fade (`.mk-clampwrap.is-clamped`, both ends of the width stated so it animates) rather than taking a row of its own, and a blank line between paragraphs is collapsed **while the caption is clamped** (`.mk-clamp br + br`), because `<br><br>` spent one of the two preview lines on nothing and printed the ellipsis on an empty row; likes, caption and comments are one block of text, so the three gaps that separate them are the same 4px; no Save as PDF; og:title `{client name} Content Review Portal by ADspace` on `review/` only. |
| Console: Content Review | Content sets folded, one open at a time; Publish / Unpublish (warn); resend for approval with a note; Google Drive import with a progress bar; S3 upload by signed PUT; only active clients listed. **Deleting a content set is `can_remove`, and the icon is drawn behind it** (`body.no-remove #deleteSet`): it takes the set's posts and their approval records with it and there is no restore, so it is the same authority as a client, a letter, a contact or a short link, and it was the one hard delete in the console whose control was drawn for everybody. A Marketing group without the permission was offered a trash icon that could not work. **A delete that removes nothing is not a success**: `db.from('batches').delete()` and `db.from('posts').delete()` both reported one. PostgREST answers a refused delete with 204 and **no error at all** — the row simply stays — so the set panel closed, nothing was said, and the set was still in the list underneath; the post delete was worse and threw its answer away entirely. Both take `.select('id')` now and name the refusal (`Not deleted. The database refused the request.`) rather than repainting a list that has not changed. `tests/setdel.js` drives all three states and `tests/stub2.js` can refuse a delete the way RLS does (`window.__refuseDelete = { batches: true }`), because a stand-in that cannot say no is one that cannot test a refusal. |
| Console: Clients (`js/crm.js`) | **One set of handles per client, not two.** The Brand profile held `social_*` and Content Review's client settings held `handle_*`, over the same row, so a handle corrected on the record left the one printed on the client's own mockup untouched. Brand now edits the `handle_*` columns the review page reads and the mockups print, and carries the **client logo** (`logo_url`) with the same disc preview, which is also the record's mark. `social_*` is backfilled by `handle_of()` — a bare handle as typed, a pasted profile URL's last segment, and **nothing at all where that segment is a route** (`instagram.com/p/DXyz` would otherwise print `DXyz` on a client's post) — and is never written again. The links row derives each address from the handle per platform (`profileUrl()`), because `https://` + `@starliving` is nothing. **Delete client** is in the record's ⋯, behind `can_remove` (`body.no-remove #crmClientMenuWrap`), a sheet that counts what goes from the record already loaded and takes the client's name typed back, plus the delete code where `delete_code_set()` says one exists; `delete_client` re-checks `allowed('remove')` when the button is pressed. Paused and Past remain how a client leaves the working list; this is for the lead keyed in twice. Lead intake (Client name, Source, Contact person, phone, email, Enquiry; Owner, Industry, Market); record in sale order; contacts as a table; Billing details fold with ring summary and billing contact from Contacts; Brand profile fold; Calls and visits with next actions and Undo; a stage clock (`stage_since`, `stage_log`, stamped by the `clients_stage_clock` trigger) showing time in the current stage under the chip on the list and the whole journey as one line on the record, with `client.stage` as its own activity tag; ageing off `STALE_H` (Lead 48 hours, Proposal sent 21 days, calendar days, Contacted unset) marking an over-run stage "2 days · Overdue" in warn and counting them in the group head; Services lines (qty × rate × months from a start date, Enquired / To quote / Confirmed, prefilled not fixed); Documents (the Letter of Offer); Engagements only once Active; stage select on the head with the Active gate. **Urgency to commence** (`clients.commence`: 1 to 3 months / 3 to 6 months / 6 months or more, blank until asked) on the intake form and the record, because the stage clock measures how long we have taken and not how long the client will wait. **The enquiry is edited like every other fact about the client**: it used to sit in the intake-only block with the contact fields, so it could be typed once at the door and never again; the contact fields stay at intake, because after that a person is a row in Contacts. **On a phone the row is a two column table**, not a card with the chip pushed right: name over meta on the left, the state chip over the age in a **fixed 120px track** on the right (measured against its worst case, "18 months · Overdue" at 119px), both top aligned. The meta line carries the deal value where there is one and omits what is not known, never a bare currency sign standing in for a figure. **Three bands, in the order somebody works them**: **Leads** first (speed to first contact is the number that moves conversion, and a lead under a hundred and eighty clients is a lead nobody rings), **Clients** (active *and* paused — a paused client is still a client and reads wrong filed under an ending, so the working book of business is one thing to scan), and **Past clients** (ended engagements, **shut by default** and remembered, because nobody opens this page to read one; they keep their place and their count rather than disappearing, since they are still clients, still hold their number, and may renew). A band is a `<button>` carrying its count, so it clears the control floor (`--ctl-h-sm`) like every other control — `.svc-cat`'s own padding left it at 42px and `uxaudit`'s `target` rule is what said so. **A filter opens every band**, because somebody who searched for a name wants the row wherever it is and a shut band would hide the one match and say nothing. Each band draws its first thirty with `Show N more` under it. **The Client ID is on the row**, under the name, in the token face: a seventh column would cost the name its width on every row for a number only read with an invoice in hand, and the phone template has nowhere to put it; it is searched alongside the two names. The ID itself is unchanged — manual, `^[A-Z0-9]{2,12}$`, unique, and load bearing, because the Letter of Offer serial is `AQL/{client_code}/{YYMM}{SEQ}`. **The list is one Client Register**, not a panel per stage: one `.crm-table.softpanel.crm-register` surface, one `.crm-head` over it, and Leads / Active clients / Paused and past as `.svc-cat.crm-band` divider rows inside it carrying their count and their overdue mark. Three panels repeated the same five headings three times, put a 24px page gap between rows of one list and pushed the third group under the fold, and made a client's stage something read from which card it sat in rather than from the column that already says it. Columns are Client, Stage, Industry, Value, Person in charge, **Last activity** (`loadLastSeen()` off `client_touches`, which the record already reads: no field, function or permission of its own, painted when it arrives so a refused read costs the list nothing) and the chevron in the column the header leaves empty. **The row is one `<button>`** with nothing nested in it, so the whole of it opens the workspace from a pointer and from the keyboard alike. Every state is a screen somebody lands on and every one is driven by `tests/register.js`: populated, loading, empty, filtered empty and refused, at 1280 and 390. **The empty states are `ADspaceState.emptyLine`**, not a private copy of the same markup. **A filter repaints only when the filter changed**: `input` and `change` both fire for one keystroke and the second arrives on **blur**, so pressing Clear the filters blurred the search box, repainted the list, and detached the button between mousedown and click — the one way out of a filtered empty list did nothing at all when it was clicked with a mouse, and a test that dispatched the event instead of clicking it could not see that. |
| Console: Clients, the record's identity and Overview (`js/crm.js` `paintIdentity` / `paintSummary`) | **The identity is two parts, and they centre on each other.** Left: the mark, then `.rec-who` (the name with its stage select on one line, the meta line under them). Right: `.rec-ctl`, one ⋯ holding **Edit** and **Delete client** (the delete item alone carries `data-need="clients:manage"`; the ⋯ itself draws for everybody). The campaign head has the same shape with **Edit details** in its ⋯. Edit used to sit beside the stage on every record, and on a phone it and the ⋯ wrapped to a line of their own under the name with the meta below them, which is the wrong reading order and the loosest part of the card spent on its rarest act; and on a desktop the controls sat on the name line, above the card's centre on every record with a meta line. `.rec-id` is `auto minmax(0, 1fr) auto` with `align-items: center`, so a long name wraps under itself and the state follows it, and the ⋯ stays on the middle of the card at every width. **On a phone the two parts are two rows** (`.is-narrow .rec-id`: `"mark . ctl" / "who who who"`): the 38px mark at the top left, the state chip and the ⋯ at the top right, then who this is under them full width on the card's own margin. Beside the mark the name had 130px, a campaign's chip wrapped under it, and the campaign's purpose line, drawn outside the grid, sat on a different margin from the name and the meta. The campaign's `.camp-purpose` is the second of three lines inside `.rec-who` (name, purpose, client · person in charge) and its chip lives in `.rec-ctl` beside the ⋯ at every width. `tests/six.js` measures the ⋯ against the card's centre at 1280, and at 390 that the mark and the ⋯ share the top row and who this is sits under them on the mark's margin; both widths assert Edit is in the menu. **The campaign's state chip is centred on the ⋯ beside it** (`.rec-ctl .chip { align-self: center }`): the shared `.chip` rule pins a chip to the top of its flex row, so the state sat 11px above the control's centre and read as squeezed in beside the dots rather than on the same line; `tests/six.js` measures the two centres at 1280 and 390. **The rate card row is the same height with or without its ⋯** (`.is-narrow .cat-row > .team-act` takes the roster's negative block margin): the ⋯ draws for an admin only, and its 44px target had made the same row 97px on an admin's phone and 68px on everybody else's. **The client's stage select is in `.rec-ctl` beside the ⋯**, the shape the campaign head already had: on the name line it stood mid line on a phone with nothing to its right. **Below 1100 the rail splits around the panes**: `.rec:not(.cportal) > .rec-rail` is `display: contents` and its blocks take `order`, so Account status, Next action, the billing gate and Profile come before the tab strip and the three `.rail-after` blocks (Key dates, Details, Recent activity) follow the pane; one rail above the tabs had put the record's own content a screen and a half down on a phone. The client portal is excluded, because its contact rows key on the rail's own width class and a rail that is not a box has none. `tests/six.js` asserts the select's centre against the ⋯ at both widths and the block order at 390. **Key dates draws no rule under nothing** (`.raildates:empty + .rail-date-edit`): every date row is omitted when the record does not hold it, so a client with none showed a heading, a gap and a hairline over an empty list. Its Save is an outlined `.btn-sm` centred on the field (`align-items: center`), not a bare word bottom-aligned against a box of another height. **The record opens on an identity area, not a title strip.** `.rec-id` carries a 46px mark — the client's own `logo_url` where we hold one, their initials where we do not (`initialsOf()`: two characters of a Chinese name, the first letters of the first two **letter-initial** words otherwise, so Dale & Cecil is DC and not "D&") — the name, the stage select, Edit, and a meta line naming the main contact's preferred language and the person in charge, each omitted when it is not known. The disc stays light in both themes for the reason the review mockups do: a client's logo is their artwork and is not ours to invert. **The Overview is an operational record**: one `.ovcard` surface of flat titled sections divided by hairlines — Contact details, Services, Letters, Calls and visits — each a heading, the one control that opens its pane (`Edit` with the pen on Contact details, `Manage services ›`, `View all ›`), and concise real rows (`.ovfacts` as label beside value in a 150px track, 104px when narrow, never eyebrow-over-value two across; `.ovtable` with stated tracks; `.ovlog` with a glyph per kind of call). Built **only from what the record has already loaded** (contacts, service lines, documents, touches): no second read, no stored number, no invented metric, no new field, RPC, permission or workflow. Enquired service lines are not shown, for the reason the letter leaves them out. A section with nothing says so in one line rather than disappearing, because "None issued." is itself an answer. **The rail is one block per question** (`.railblock`, each with a `.railtitle` at the pane's own 15px section size, not an eyebrow): **Account status** (the stage chip and the stage clock, `12 days in this stage`, with `· Overdue` in warn where `isStale()` says so; Active for left Key dates when this arrived, so the clock is stated once), Next action, the **billing gate** (`.railgate`: a red `--err` block, drawn only while a billing field is missing on a record not yet Active, a button that opens Billing where `maySeeBilling()`, plain otherwise; it was an amber `.notice` reading "Not active." on every lead, which is a caution's colour on a refusal), Profile (the completion bar), Key dates, Details, Recent activity. Key dates and Recent activity rows carry a neutral 16px glyph (`RICON` / `ico()` in `js/crm.js`: calendar, phone, pin, person, file, tag, image, speaker, link), and the Overview's calls do too, in a 30px `--sunk` disc. **Next action prefers the one a person wrote** (`client_touches.next_action` / `next_at`, overdue in warn) over the step `nextStep()` derives, because a person decided it and a derivation did not. **Record completion is a bar with the missing list under it**, never a bare figure, counted over billing, brand, contacts and services. **Key dates omit a row whose date is not there** (Client since from `created_at`, Last contact and Next follow up from touches). **Recent activity is the last three `activity_log` rows**, read once when the record opens and kept in `state.log`, so the Activity pane no longer re-reads. **A block leaves entirely when its data does not exist**, so the rule under the last one is a class `paintRail` sets: `:last-child` counts a hidden sibling. Person in charge is on the identity line and Added is in Key dates, so neither is repeated in Details. **`.rec` states `grid-template-rows: auto 1fr`**: the rail spans both rows, so with `auto auto` a rail taller than tabs plus pane had its extra shared between them and a lead opened with sixty pixels of nothing between its tab strip and its first section. |
| Console: Services (rate card) | Two tables, Services and Add-ons, categories as sub-headings **carrying their count**; admins edit, everyone reads. **One heading, Price, over the amount and the unit**: the unit qualifies the price (`RM 360.00  Per post`), so Rate and Unit were two headings over one value; the cells stay two tracks so every amount keeps the same right edge. **Active costs no column and no accent**: nearly every line is active, so the row is the name and what it costs, `Inactive` is a neutral chip beside the name with the row faded, and Set inactive / Set active sit in the ⋯. On a phone the row is two lines (name and price, then the unit), not three. Rates in RM. A row carries `detail` (what it includes, one item per line) and `min_months`, both of which seed a client's service line and stay editable there. **Inactive, then Delete permanently** in the ⋯ (`is-danger`, no `data-soft`, so `body.no-remove` holds it back), refused while any live `client_services` line names the slug and saying how many. The schema **seeds the whole card once**, into a database that has none, and **seeds `detail` once** (`where detail is null`): the card is edited in the console by the people who own it, deletions included, and a seed that ran on every re-run would put a deleted line back and overwrite their corrections the next time a schema change shipped. A line added to the seed list therefore never reaches an existing database; a new service is added on the Services page. `tests/sql.js` runs the seed twice against a real Postgres and asserts a deleted line stays deleted. |
| Console: Creator Campaigns, the draft step | **A card waiting on us opens by itself.** A creator submits and the step becomes Submitted; folded, the card was a name, a chip and a summary line, so the files they sent, the caption they wrote, **Release to client** and **Request changes** all sat behind a fold nobody knew to open, and production read as a booking with no next action at all. `submitted` now opens on load (a deliberate fold is still remembered, because `openCards` stores the `false`), carries `.is-waiting` as a warn left edge so a folded one still reads as needing somebody, and leads its folded summary with `2 files` — how much arrived is the first thing anybody wants before opening it. `.is-waiting` had been a class nobody styled, set on `reviewing`, which waits on the client and not on us. The console's own thumbnail now falls back to the file type the way the creator's page always has, instead of drawing the browser's broken image mark over a creator's work. | **A creator's video plays here.** The console drew a submitted video as a square with its file extension on it, so the only way to watch the work before releasing it to a client was to download every file — on the one screen whose whole job is to look at it first. `.filecard-video` carries a real `<video controls playsinline preload="metadata">` at 9:16, letterboxed on black rather than cropped, with the × the client's page has no business carrying; a stylesheet rule for exactly that player had been in `css/portal.css` matching nothing for months, which is how the missing markup went unnoticed. **Media is watched and everything else is opened, so they are two shapes**: video and image are 9:16 cards in `.filegrid` (both, so the rows are even), and anything else is a `.filepin-row` attachment line under them — a 9:16 player beside a square tile left the third file orphaned on a row of its own, and a PDF drawn at 9:16 to even it up is a tall empty box. **Exactly one step on a card is blue**: `advance` draws `.btn-go` only for `reviewing` (Release to client), because blue moves work to somebody else and the other five steps are the team recording its own progress; five creators each carrying a filled blue forward button made a pane where nothing said which action mattered, and broke the one-blue-action law the audit counts. `tests/prod.js` asserts the player, the two shapes and that only Release to client is blue.
| Console: Creator Campaigns (`js/campaigns.js`) | **Next means next.** The rail's Key dates took the earliest shoot of every live booking and called it the next one, so a date that had passed went on being promised — 12 Sept still reading as the next shoot in October. It is the earliest date from today onward; where every date is behind us there is no next one and the row reads **Last shoot** rather than claiming something that is not coming. The dates sit in the `--sunk` chip `.readfield` uses, because a flat list of labels gave the figures the same weight as the words beside them. **Pending visit is a step, not the absence of a date.** `nextAction()` read every booking at `pending_visit` as "N shoots have no date yet", so a campaign with every shoot in the diary told the team two were unscheduled. It counts the undated ones (`no date yet`), else the dated ones ahead (`2 shoots are scheduled, next on 20 Sept 2026.`), else the ones that have passed and were never moved on (`… have passed and still read Pending visit.`); `delivery` / `deliveries` for a seeding campaign. `tests/six.js` sets every booking to Pending visit with a date and asserts the line names the next one, then blanks one and asserts it is the one counted. **A date field is a field, not a button** (`.input[type=date]`, `.input[type=time]`, in `css/portal.css` beside the file field): `appearance: none`, the value left aligned through `::-webkit-date-and-time-value`, `display: block` so an empty one keeps its height on iOS, a calendar or clock glyph on the left and no picker mark on the right under a coarse pointer, Chrome's mark kept and quietened at a desk. The phone shoot row is `minmax(0, 1fr) 122px`, measured against `10:00 AM` at 16px behind the glyph. **A date nobody has set says so** (`.sched-one.is-unset`): an empty `input[type=date]` draws nothing at all on iOS, no `mm/dd/yyyy` and no caret, so an unset Publish date was a blank pill under a label with nothing saying it was a date field. The hint replaces the browser's own placeholder (`::-webkit-datetime-edit` at `opacity: 0`) rather than printing over it, and leaves on focus. **It hangs off `.sched-field`, a wrapper around the field and nothing else.** Hung off the cell it was centred on the label as well: on a phone `top: 50%` of a 65px cell is 10px above the centre of the 44px field inside it, so "Not set" printed across the box's own top border. The phone rule written to correct that re-anchored to `bottom: 0` and restated the height from `--ctl-h-sm` — a second guess at a box already on the page, 6px out at the desk where the field is 38px and the token is 32, and silently wrong wherever the first guess was. A wrapper needs neither guess and the original `top: 50%` is then simply right. `tests/sch.js` measures the hint's centre against the field's at 390, 768, 1280 and 1440. **A narrow Schedule row is two lines of fields, not three.** Each booking stacked Shoot, Draft due and Publish one per line with an 11px label in a 72px track beside each, so five creators drew fifteen full-width date boxes and the pane read as a form rather than a schedule. Draft due and Publish share a line, every label sits **above** its field as it does everywhere else in the portal, and the shoot keeps its date and time together under one label. The cells are `align-items: start`, or a field stretches to a row sized by its neighbour and draws a 90px box. **The booking register clips its own rows** (`.bookreg { overflow: hidden }`): every `.kcard` inside carries a white background at `border-radius: 0`, so the last row painted a square edge over the register's two rounded corners and an open row's sunk step backgrounds painted over them again. Rounding the row's last child chased the symptom and kept missing whichever element actually owned the bottom edge. Nothing is clipped by it, because `ADspaceMenu.place()` puts every row ⋯ on the viewport. **One bulk-date panel, moved to whichever pane asked for it**: `#bulkBox` is markup inside the Creators pane, so Schedule's own Bulk dates opened it inside a pane nobody was looking at — the button appeared dead and the form was waiting on the Creators tab afterwards. `bulkOpen(btn)` moves it under the pressed button's `.viewhead` and opens it there; a second press on the same head closes it. Schedule's copy of the action hides with `#bulkToggle` when nothing is in production. **Add creators runs two jobs and names both**: `Existing creators` (a labelled search filling its section, a count of what is still addable, and a bordered scrolling list) and `New creators` (Add creator, **folded shut**, because it is the rarer path; a restored draft opens its own fold, or the half typed creator is there and invisible). Every input in it carries a name, including the per row rate and the profile link. **The word is Creators List, never "roster"**, on screen and in the section labels; the identifiers (`rosterView`, `loadRoster`, `?tab=roster`) are addresses, not copy, and stay. **A missing profile link is asked for where it is missed, and the tick is what opens into it**: ticking a platform the creator has no link for grows that box into the field (`.pbox` > `.pbox-tick` + `.pbox-link`, `needs-link`), and Add saves what is typed into `creator_profiles` through the same `readProfile()` parser the Creators List uses. Before it, ticking a platform meant leaving the campaign, adding the link on the Creators List and coming back, so the tick went in and the link never did; the first fix put the field in a labelled row **after the Add button**, which is downstream of the control that sends it and reflowed the whole row on every tick. One card per creator, folded; Confirm creators; steps Confirmed → Pending visit → Pending draft → Reviewing → Changes requested → Scheduled → Posted → Completed, each gated by its data (draft link, publish date, posted on or after the date) with Revert; withdraw / replace / reinstate in the ⋯; In production is derived, not stored, so reverting, withdrawing or replacing the last creator puts the campaign back to Open for selection; bulk dates; campaign due date; the Invoice fold appears only once a creator is confirmed and leaves again if the last confirmation is reverted, one Save / Cancel covering the number (`AINV` + six digits) and the PDF together, with Remove PDF and Undo; `get_campaign` withholds `invoice_no` and `invoice_url` until then, so the client cannot read an invoice it has not yet caused; results as a table grouped by platform; Publish to client / Unpublish. |
| Console: Creator roster (`js/campaigns.js`) | Creator · Profiles · Fee · ⋯ on the console's own record row, never the Short Links `.slink` it used to borrow (which set a person's name in the slug's monospace face and pushed two icon buttons onto a line of their own). Edit / Set inactive / Remove in the ⋯; `creators.active` is surfaced at last, so the refusal that says "Mark them inactive instead" now points at a control that exists. A profile is a `.plink`, the same outlined button with the leaving-the-page icon that the client's own page uses, never the 999px `.pchip` pill it was: a pill is the shape this portal gives a state, so a link drawn as one read as a status somebody had set rather than as something to open. It shows its handle only where the handle reads as a name (18 characters or fewer), because rednote stores a profile id there. Search is name and handle; the placeholder no longer promises industry, which is neither stored nor searched. **Built for four hundred, not for three**: a platform filter cuts the list on the axis a campaign fixes first, fee bands (`Up to RM 300`, `RM 301 to 500`, `RM 501 to 800`, `Above RM 800`, `On quote`) divide it as `.svc-cat` sub-headings with counts, and inactive people are their own band at the foot. **Creator · Profiles · Campaigns · Fee · ⋯, a table with columns.** A list whose every value is stacked against the left margin has to be read line by line before it can be scanned, and `#rosterList` had no `.crm-table` at all, so the rows sat straight on the page ground with no card behind them. The **monogram disc is gone**: it was there to make a row findable by eye and on this roster it cannot, because most names are Chinese and a first character gives 是yy呀 and 是甜甜啊 the same circle, which is the identical failure that had already ruled out an alphabetical band. The **record** (`4 · last Aug 2026`, `None yet`) is counted in `loadRecord()` off `campaign_options` from `confirmed` onwards, and is what differs between two rows on a roster of one fee and one platform. There is deliberately **no "on a campaign" mark**: a creator is often on several at once, so the chip was true of nearly every row. The **profile is a link on the row** (`.plink-bare`: the platform, the handle where it reads as a name, and the mark that says it leaves the page) and not an item in the ⋯, because a creator is looked up constantly. On a phone the row is two lines (name and fee, then the links with the record at the end of the same line) at 80px, and the ⋯ carries a negative block margin so its 38px target stops setting the first line's height. |
| Creator portal (`creator/`, `js/creator.js`) | **A finished booking asks how it went and stops asking for money.** Payment details are named at `scheduled` and `posted` and leave at `completed`, because a standing call to action on a closed job reads as something still owed; in its place is **Your experience**, five stars written to `campaign_options.creator_rating` by `creator_rate` (1 to 5, refused at any other step, changeable while the booking stays completed, never shown to the client). **A submitted file is an attachment line, not a tile** (`.filepins` / `.filepin`: the paperclip, the name, the size): the page can play none of it once it is handed in, so a wall of 9:16 squares showing a file type and a truncated name spent the screen saying nothing. Tiles stay while the creator is still assembling the hand-in, where they are things to look over and take back off. **The draft countdown is amber, and red once it has passed** — it was blue, which is this portal's word for "press this", spent on a date nobody can press. **The Sign out button is named and hidden until there is a session**: it was created empty and labelled only when a booking painted, so the sign-in cover carried a nameless button. **The one party in this operation with no page of their own.** Drafts arrived in a Google Drive folder somebody had to find and paste a link to; "when am I shooting" was asked on WhatsApp. Signing in is **a code, not an account**: `creators.access_code`, eight characters from a 31 character alphabet with no 0/O/1/I/L in it (31^8, six orders of magnitude past the six digit code a bank sends by SMS), carried in the link so one tap is enough and kept in `localStorage` so it is typed at most once, on a new phone. Not SMS OTP: the agency holds phone numbers rather than addresses, a message costs money every time, and a freelancer has no password to remember. Reset in the creator's record, which is the only place the code and the link are shown; the old link stops working and it asks first. **A creator sees their own booking and nothing else**: the campaign, the brand, the brief, their platforms, their fee, the dates and where their own work has got to. Never the client's stage, the campaign's commercial state, the client's amount, who else was offered it, or the team's notes on the row, and never a campaign still in `draft` or an offer they have not been confirmed on — withheld by `get_creator`, never only by the page. **Handing in** is open at `pending_draft` and `changes` only: files go straight to S3 by the same signed PUT the console uses (`sign-upload` takes a creator code and an option id instead of a session, checks them with `creator_may_upload`, and builds the key from the option it verified so a real code cannot be pointed at another booking's folder), `creator_submit` moves the step to Reviewing and keeps the caption they wrote, and a file can be taken back off until it is handed in and not after. **The Draft step names both routes into it**: a `?` hint that opens by itself the first three times and then retires behind its own mark (`hint()` / `bumpHint()`, counted in `localStorage`), because an instruction is right while a screen is new and furniture ever after, and a `title` tooltip cannot be reached on a phone. The paste field is labelled as the fallback it is (`Or paste a link, if they cannot upload`), and **Copy portal link is an icon button beside the ⋯ on each creator's card**, because sending a creator their link is the everyday action and it was three steps inside their record. **A value that is only read is not a field to type in** (`.readfield`): the creator's code, their link and the client links were readonly inputs drawing the same box as every editable field, inviting a change that cannot happen and hiding the one thing anybody wants to do with them. **AP01 is named on approval, not on submit**, because a creator who has to reshoot would otherwise have filed payment details against work nobody accepted and the correction lands in the ledger. English and Chinese; covers for the code, an invalid code, a creator stood down, and a page that could not load. **The upload reports itself**: up to **300 MB a file** (`ADSPACE_CONFIG.s3.maxUploadMB`, refused by name before a byte moves, because a file turned away after a ten minute upload is a file uploaded twice), several files a booking, the progress Content Review already draws (what is going up on the left, how far on the right, `Saving` once every byte is out and S3 has still to answer), Submit shut while anything is in flight, one bad file never abandoning the rest of the batch, a two minute silence ending the attempt with a reason, and the outcome written onto the card **after** the repaint that follows it rather than before, or the redraw throws the one line explaining what happened away. It failed in production twice on two defects that compounded: `creator_add_file` declared a variable called `id` and then looked the booking up with an unqualified `where id = p_option`, which PL/pgSQL raises **at run time and not at create time**, so the schema applied cleanly; and the page threw that error from inside a fulfilment handler whose sibling rejection handler cannot catch it (`.then(ok, bad)` — `bad` never sees what `ok` throws), so nothing was said, the bar stood at 100%, and Submit then refused because no file had been recorded. `tests/sql.js` runs the whole delivery path against a real Postgres and `tests/creator.js` makes the database refuse a save and asserts the page names the file. |
| The name a decision is recorded under (`js/decide.js`) | **Asked on the page, never in a browser dialog.** Both client-facing decisions record who approved them and both used `window.prompt`, which this portal's own rules already rule out for anything that has to be typed: it cannot be styled, it cannot be translated (the buttons stay in the browser's language, so a Chinese reader got half a dialog), and on a phone it is a system sheet that takes the reader off the page they are deciding on. The field grows out of Approve instead — the `.pbox` pattern, a control that creates a need answering it in place: `.namebox` > `.btn-approve` + `.namebox-field`, `max-width` 0 to 100% because a stated end animates and `auto` does not. Approve is its own confirm, Enter sends, Escape puts the row back, and a name already held opens nothing. **Request changes steps aside while it asks** (`.approve-row.is-asking`), because by then the client has chosen and this is the confirmation of that choice; left in place the field had 135px to grow into at 390 and the row wrapped mid animation. **Where the note box is already open it carries the field itself** (`.changebox-who`, drawn only when no name is held), since the same question belongs where the client is looking. One copy for both pages, which kept private helpers over the same `adspace_reviewer` key. |
| Client-facing creators (`creators/`, `js/creators.js`) | **The decision is two buttons and nothing else.** Approve and Request changes; the note box opens only when Request changes is pressed and refuses to send empty; the name is asked once by prompt at the moment somebody decides (`adspace_reviewer`, the key `/review/` uses) rather than sitting as a field on every card, because a reader who is not deciding anything was being asked to fill one in. **A decision leaves a record.** Approving used to move the booking to Scheduled and say nothing at all: no activity row, and nothing on the card naming who had approved it. `review_draft` now writes to `activity_log` under the reviewer's own name, `get_campaign` sends the last `option_reviews` row with the booking it is about (withheld with the draft, like the files), and the card keeps `Approved by Wei Ling on 16 Sept, 10:47.` — with the note under it when changes were asked for. **A draft is decided on the card, never behind a window.** The client reads the creator's work in place (`draftPreview`: the video or image at 9:16, the caption, and the pasted link where there is one) and Approve and Request changes sit under it, so the decision uses the same `.approve` component Content Review uses — the same blue Approve, the same quiet Request changes, the same note box opening under them. It used to be a `Review draft` button that opened a sheet over the card, which meant a frame to open and a frame to dismiss before the client could say anything, on a card that was already showing them the thing they were deciding on. The name a decision is recorded under is kept in `localStorage` as `adspace_reviewer`, the same key `/review/` uses, so a client who has approved a post already has their name in the field; it stays optional here, as it always was. The decision draws only while the booking is `reviewing`, so approving repaints it away and the chip is the confirmation. `#draftSheet` is deleted. **The offer ends when the slots do**: once every slot is booked the creators nobody picked leave the page rather than standing there as a column of disabled ticks, and the progress card goes with them; they come back by themselves if a slot reopens, because the count is what decides. **Backups are the team's to offer** (`campaigns.backups_open`, off by default, a switch on the campaign): with it off there is no Backup control at all and `save_selection` refuses one anyway; with it on the list survives a full campaign under a `Backup creators` head. **A fee is quoted for a stated set of placements**, so every selection row names them (`Posting on rednote · Instagram`) as the meta line under the creator's name: one creator posts to two platforms for the same fee and the next charges for the second, and a rate with nothing to read it against is a number the client cannot compare. It is not the same list as the profile buttons beside it, which are where the creator is looked up. The console stores the placement by its printed name (`rednote, Instagram`), so `platsOf()` maps it back to the key before looking the word up, and a Chinese reader sees 小红书 on the selection row and the booked card alike. Selection with optional backups (recommended, never enforced); `View rednote Profile ↗`, `View Instagram Profile ↗`; same status words and colours as the console; foldable amount card with View PDF; results readable on a phone without sideways scroll; English and Chinese. |
| Asking for one value (`js/ask.js`) | **A browser dialog is not a control this portal has**, and it was reached for eight times anyway because it is one line of code and the right control is twenty. Those twenty lines, written once, in two shapes chosen by what the value **is**. `rename(host, btn)`: the value is already on the screen (a content set's title), so the thing itself becomes editable where it sits and the pen becomes the tick that saves it — Enter sends, Escape puts it back, an empty name keeps the field open, and leaving the field neither saves nor discards, because walking away is not a decision either way. The tick takes the **ink** fill and not blue, since Publish is two controls along in the same bar. `inline(btn)`: the value is not on screen yet and one control needs it, so the field grows out of that control and it confirms — the `.namebox` the client pages use, with a seed that may be a function (the month a new set is named after). `note(after)`: several lines somebody else will read, so a textarea opening under the control that sends it, which is the shape `/creators/` Request changes already had. **There is deliberately no sheet in it**: where an act has a consequence that must be stated before it is agreed to, a field growing out of a button hides exactly the part that matters. `tests/sets.js` drives the content set flow — which nothing covered, which is why two prompts lived in it for months — and fails on any browser dialog opening at all. |
| Copy (`js/copy.js`) | **A copy button copies, and says so.** The fallback for a refused clipboard was `window.prompt`, which put a browser dialog with an OK and a Cancel in front of somebody who had pressed a button labelled Copy, and copied nothing unless they also selected the text themselves. It fired far more often than "outside a secure context" suggests: the Clipboard API rejects whenever the document is not focused, which is every press made while another window had focus. `document.execCommand('copy')` over an off-screen textarea is the second path — no permission, every browser, synchronous — and whichever one works the button says Copied. No browser dialog. |
| Short Links (`workers/links/`) | **No Status column.** Live is true of nearly every row, so a STATUS heading stood over a cell that was empty almost always and read as something broken rather than as the accent rule working. The rate card had already settled this: where a state is the default for its list, the row says nothing while it holds and names the exception beside the name. `Paused` is a chip next to the slug and the row fades, exactly as an inactive service does; four columns, not five. **The list is live.** `hi.adspace.me` is a Cloudflare Worker on the ADspace account (`adspace-links`, custom domain on the `adspace.me` zone, which is what creates the proxied `AAAA 100::` record and the certificate); a row saved in the console redirects as soon as it is saved and a row paused there stops redirecting. The "Not live yet." `.routenote` under the table is gone, because a standing fact that is false is worse than no line. **The Worker holds no key that can write anything**: it calls `link_resolve(p_slug, p_qr)`, a security-definer function granted to `anon` that answers one exact slug with one destination and one state — no listing, no search, no prefix match, no second column — so the public key is enough. The alternative, written into the schema for months, was a service role key in a Cloudflare secret so that a redirector could read one column. What this gives away is what a short link gives away by definition. **Four answers, because they are four pages**: `ok`, `missing`, `paused` (the team turned it off), `revoked` (a pulled QR code, turned away while the typed link keeps working, which is why `link_qrs` carries its own identity). **302 and never cached**: the reason these live in a table is that a destination can be corrected, and a 301 would mean the correction never reached anybody who had already followed the old one. Parameters a client appended to a printed link are carried onto the destination, minus `q`, with the destination's own parameters winning. **The host is one value, `ADSPACE_CONFIG.linkHost`**, read by `js/admin.js` and written into the field's prefix; it was typed into the script and into the markup and the two could disagree. **`go.adspace.me` is deliberately not retired and is not served by this Worker**: a QR code encodes the whole address, so the twenty or thirty already printed on slides keep working for as long as that host redirects. `tests/links.js` drives every answer against the module with `fetch` stubbed, because the sandbox's network policy refuses `hi.adspace.me` exactly as it refuses `digital.adspace.me` and `curl` proves nothing either way. |
| Access: a level per section (`supabase/schema.sql`, `js/admin.js`, `js/team.js`) | **Access is a level per section, not a switch per verb.** `team_roles.access` / `team_members.access` is a jsonb map over seven sections (clients, review, campaigns, links, services, team, activity) with four levels ranked **none < view < work < manage**, and `allowed(section, level)` is the predicate every policy asks. The old model was six section booleans plus one global `can_remove`, so the authority to destroy could not be granted for one section without granting it for all: turning it on so Marketing could delete a **content set** also let them permanently delete a **client**, a letter, a contact, a rate card line, a short link and a creator. **The levels are drawn on reversibility, not on add/edit/delete/share**: add and edit are reversible and so is publishing (Unpublish exists), a permanent deletion is not. A matrix of sections against verbs is twenty eight switches a group, about sixteen of which name nothing this portal does — and this console had already removed one permission matrix for exactly that reason. **One capability stays a switch, because it is not a section**: `can_billing` is a pane inside the client record. `can_doc_void` was a second one, voiding a verified letter kept apart from deleting it, and was folded into **Clients: Manage** on 2026-09-22 at the user's request: the person trusted to delete a client's letter is the person trusted to void one, and two switches for one level of trust left groups with Manage and no void. The columns stay, unread; `supabase/migrations/2026-09-22-void-is-clients-manage.sql` redefines `allowed(text)` and `letter_set_void`. The policy block reads the levels directly — select is `view`, insert and update are `work`, delete is `manage` — so the four statements are the four levels. **The one-argument `allowed(flag)` survives for `billing` and `admin`, and a bare section name in it means `work`**, so a call site missed in the cutover refuses rather than quietly granting a write; `allowed('remove')` is gone, so anything still asking for it fails loudly. **`allowed()` had been defined twice** — the second copy existed only to reach `is_admin`, declared below the first — and that copy had dropped `doc_void` from its case list, so the capability answered false for every non-admin group however the Team page set it; the columns move up and there is one definition. On the page, `may(section, level)` and one body class per section (`no-manage-review`, `no-work-clients`, …) replace `no-remove`, and **every destructive control names its own section** (`data-need="review:manage"`) instead of the stylesheet guessing from `.is-danger:not([data-soft])`. The Team panel is one select per section over two tickboxes, and the row reads back grouped by level (`Manage: Content Review · Work: Clients · View: Services`). **The levels are backfilled once from the booleans each group already held**, guarded on emptiness so a re-run cannot undo what somebody has since set: a section the group could open becomes `work`, or `manage` where it also held the old remove flag, so the day the file runs nothing anyone can do changes. `tests/levels.js` proves the ladder, both signatures, the two capabilities, the admin, the stood-down colleague, the stranger and the backfill (twice) against a real Postgres. |
| Team (`js/team.js`) | User groups (`team_roles`: Admin, **Marketing**, Sales, more can be added; the group that handles clients was named Account, which is the word for a login, and the rename is a guarded one-line migration on the old name because the slug `account` is what `team_members.role` points at and never moves) carry the seven `can_*` switches and `is_admin`; a person is in exactly one group. **Members are listed under their group** as `.svc-cat` sub-headings, the way the rate card lists services under a category, so the row is Person · State · ⋯ and no longer repeats the group on every line; a group nobody is in draws no heading, and a person whose group was deleted falls under "No group". Active first, then by name. **A group states what it opens in words** (`Clients · Content Review · Billing`, `Everything` when `is_admin`, `No access` when nothing), so there is no column per switch and no table wider than the console; the switches live in the panel that adds a group and edits one. The Admin group has no ⋯ at all. **Your own row is marked `You` in the neutral chip**, not a word in the accent green: being you is a designation, and green is spent on a live state. Edit in a member's ⋯ opens the same panel that adds one (name, sign-in email, group), and changing the email asks first, because the row's email is the address the console signs in with. **Send invitation** asks first and sits one place from Edit, and **Set inactive / Set active** sits below it (never on your own row), because standing somebody down happens once in a job and a green Active select on every row spent the accent on the ordinary case. `invite-member` edge function. |
| Precision Workspace (`feat/precision-workspace`) | **Every console list is found the same way.** `.cmdbar` carries a search box with its glyph (bounded 190 to 420px), the filters, a count and the one action, on Clients, Content Review, Creator Campaigns, the creators list, Short Links, the rate card and the Team; the count reads `7 services` whole and `3 of 41` once a filter is on, and on a phone it shares the action's line rather than taking a row of its own. **`js/state.js` is the only copy of loading, empty and failed**: `skeleton()` draws the shape of what is coming, `emptyLine()` carries the way out, `failLine()` names what could not be loaded, what the database said, and offers Try again — a read that failed is not an empty list, and "No content sets.", "0 posts" and "Nothing added yet." were all printed over failed requests. **Two corners, 14px on a bounded section and 10px on a control**, and nothing between them. **The client record's section actions are outline**: four ink slabs down the right edge made the way to add records the heaviest thing on a page of records, and on a phone a heading shares its line with its one action. (**Superseded in Phase 2**: the record is panes now, so each shows exactly one section action and the wall of four cannot arise. Add contact, Add service, Issue letter and Add entry are the blue primary; the phone rule stands.) **An empty list is a line inside the panel it would have filled**, never a dashed rectangle with the words centred. **A fold keeps its summary on a phone** — "0 of 4" is what says whether billing has been filled in. **A state is a chip with a word in it on `/review/` too**; the coloured disc beside a word of the same colour is gone from the review cards, the Drive import rows and the saved posts. **16px body on the four client facing pages** (`:root[data-face="client"]`), 14px in the console. **A campaign whose name would render as nothing** (empty, whitespace, `0`, `"null"`, `"undefined"`) is drawn as `Untitled campaign` and stays editable, and a new one carrying such a name is refused on save; the client's page and the client portal use the same fallback in both languages. `.slink` and `.linksbar` are deleted. |
| Precision Workspace, the structural half | **A record is a workspace with a rail, not a column.** The client record is identity at the top, seven panes (Overview, Contacts, Billing, Brand, Services, Documents, Activity) and a rail carrying the facts that are true in every pane; the campaign record is identity, one line saying what it is waiting on us for, and seven panes (Overview, Creators, Schedule, Deliverables, Client selection, Finance, Activity). The pane travels in the address (`tab=` for a client, `pane=` for a campaign) and **pushes** a history entry, so Back and Forward walk the record while everything else in the console still replaces; Overview is the default and stays out of the address. A pane is the disclosure, so opening Billing opens the fields, and the refusal that blocks Active lands there with the first missing field focused. Re-opening the same record keeps the pane somebody was working in. **The campaign's next internal action is derived on every repaint**, never stored, so reverting a step cannot leave it lying. **Schedule, Deliverables and Client selection read the bookings and never write**: they are views over `campaign_options` and `campaign_deliverables`, not second stores. **Activity keys on the subject** `activity_log` was written with, which is the client's or campaign's name at the time, so a rename leaves older entries behind; that is stated in the code rather than papered over. **The account leaves the sidebar**: who you are, the register and Sign out sit behind one control at the end of the console bar, opening on the address, the theme and Sign out, closing on Escape or a click outside. **A creator is added or edited in a sheet**, so four hundred rows are not pushed down the page. **`/creator` is a work queue and one open booking**, ordered by what has to be done, marking the one waiting on them, with the booking in `location.hash`. **`/creators` is a comparison on a wide screen**: named columns (Creator, Posting on, Profiles, Fee, Backup), every track stated so a column starts at the same x on every row, and back to the stacked row below 760. **`/review` keeps the gallery and adds the Review Canvas**: the mockup at size on a stage, and the status, the copy in full, the note and the one decision area in a rail, with prev/next, the arrow keys and Escape. The canvas **moves** the card's own blocks and puts them back on close, so there is one approve control and it cannot drift. **`/client` takes the same rail**, with Overview and Request change heading the facts they name. |
| Precision Workspace Phase 2 (`feat/precision-workspace-phase2`) | **Blue means action.** `--action` / `--action-hover` / `--action-pressed` / `--on-action` / `--action-ring` in `css/portal.css`: the filled primary, the forward action and the client's Approve are `#0b57d0` (6.39:1 on white; Apple's `#007aff` is 4.02:1 and was refused), green is left to live state and success, amber to caution, red to destruction, everything else neutral. One prominent blue action per view, two at most, counted by `uxaudit`'s `accent` rule over `.btn-go`, `.btn-primary` and `.btn-approve`. Hover, pressed, `focus-visible` and disabled are all stated, and every `.btn-*` hover sits inside `@media (hover: hover)`. **The campaign record opens on the work**: the same identity area the client record uses (client's mark, name, real state, Edit, a meta line naming the client and the person in charge), an Overview of flat titled sections over the bookings, the schedule and what has been handed in, and a rail of one block per question (Selection with a bar, Amount right aligned with the total under its hairline, Key dates, Client link, Details). The six statistic cards are gone; what the campaign is waiting on us for stays the warn line under the identity and is deliberately not repeated in the rail. **The bookings are one register** (`.bookreg`): five stable cells on a stated grid, a header over them, hairlines between rows, `.kcard` keeping its steps but losing its border. **Creator Campaigns and Content Review are registers**, not card grids, with the campaign states as `.crm-band` divider rows. **Every console route opens on its command bar**; a heading that only repeated the bar or the tab above it is gone and `.console-body` carries a 20px step so every route starts at the same y. The Short Links notice is a `.routenote` line under the register. **The client portal opens on an identity area** (mark, name, state, one blue action) with the rail's blocks flat under eyebrow titles and the reach links de-pilled; Portal access is green and Main contact neutral, matching the console. **The creator's queue is one surface** with a warn edge on the booking that needs them. **Creator selection is one summary band**, and its profiles column is left aligned so every row starts at the same x. **The client record's one-per-pane section action is the blue primary** (Add contact, Add service, Issue letter, Add entry), superseding the outline rule, which was written when the record was one column and four of them ran down its right edge. **A rule about something inside the record pane keys on the pane's own width** (`ADspaceState.fit` → `is-narrow` ≤640, `is-tight` ≤460, from a `ResizeObserver` on `.console-body`, `.rec-pane` and `.rec-rail`), never on the window's: at 1280 the pane is 591px, so every `@media (max-width: 640px)` rule inside it was false exactly where it was needed and the Services name track collapsed to 59px with the description one word per line. Container queries are deliberately **not** used, because `container-type: inline-size` implies `contain: layout` and would become the containing block for the `position: fixed` row ⋯ menus. The services row states `minmax(180px, 1fr)` for its name and stacks below the width that can honour it; the booking register is one line down to 460 and two below it, never three; the tab strip never wraps and scrolls instead, with `flex: 0 0 auto` on each tab; the account menu states its own 260px width, `flex-direction: row` and left alignment, outranking `.kmenu`, which comes later in the file; Copy link and Preview are neutral, because blue is spent on the one thing to press. `tests/geom.js` asserts all of it at 1440, 1280, 1024, 768, 390 and 320. |
| The way back is where the act was (`js/campaigns.js` `undoBar`) | **A safeguard nobody can see is not one.** Taking a creator's handed-in file off a booking has always been a **soft** remove (`campaign_deliverables.removed_at`) with an Undo — but `#campUndo` is one bar at the top of the campaign record, above the pane strip, while the × sits inside a creator's card most of a screen further down. Measured at a 900px window: the × at y=434, the bar that offered the file back at **y = −223**, entirely off screen. The file vanished from under the pointer and nothing visible was offered, which is what "there is no safeguard" describes on a control that has had one since it shipped. `undoBar(text, undo, host)` takes the element the act belongs to and draws an `.undobar-here` directly after it, removing it again when it is used or times out; the handed-in file passes its `.handedin` block and the invoice PDF passes `#invMsg`, because the Finance pane is inside the record too. It also **names the file that went** (`reel-final.mp4 removed.`), since "Submission removed." over a grid of three said nothing about which one. The removal takes `.select('id')` like every other write that a policy can refuse, so a refused update is named rather than costing the card a file the database still holds. `tests/undo.js` measures the bar against the window, drives Undo to the file coming back, and makes the stand-in refuse the update (`window.__refuseUpdate`). **There is deliberately no confirm**: the portal's law is soft remove plus Undo, and a dialog on every file removal would tax the everyday case (tidying a wrong upload) to protect the rare one — the Undo does that better, once it can be seen. |
| Row ⋯ menus (`js/menu.js`) | `ADspaceMenu.place()` and `ADspaceMenu.onScroll()` are the only copy; `crm.js`, `team.js`, `portal.js` and `campaigns.js` keep their own click wiring (what a menu holds and what it is scoped to differ) and call in for the rest. Four copies of the same twenty lines had already drifted, and the fix below had to be made three times and still missed the fourth. Placed on the viewport by their button so the table cannot clip them; **opened upwards where the room is above**, or a ⋯ on the last row opens past the bottom of the window. The creator card inside a campaign was the one menu that never called `place()` at all, so it stayed absolutely positioned in its card and a creator low on the list opened a menu running off the bottom of the window; `tests/keyin.js` could not click the third card's menu item and had been red on `main` since. The scroll that closes an open menu ignores **the scroll the browser fires to reveal the button it just focused** (`held` / `scrolledAway()`: a scroll that has not moved the anchoring button by 2px is not a reason to close). Without that guard the ⋯ closed itself the frame after it opened, and on a phone the menus on the lower rows could not be opened at all. |
| Activity record: the client's and the creator's side (`supabase/migrations/2026-09-21-…`) | **The record answers a dispute, and it held one side of every conversation.** A client approving a post wrote to `reviews` and nothing else; a client confirming a set of creators wrote to `campaign_confirmations`; a creator handing work in and rating a finished booking wrote nothing at all — so the one screen anybody would open to ask "who approved this, and when" showed only what the team had done. Five security-definer functions now log what they already do, under **the name the person typed** as the actor: `submit_review` (`review.approved` / `review.changes`, with the note), `confirm_selection` (`campaign.confirmed`, with how many), `creator_submit` (`campaign.submitted`, with how many files), `creator_rate` (`campaign.rated`) and `portal_withdraw` (`request.withdrawn` / `request.reinstated`). **`save_selection` is deliberately not among them**: it fires on every tick as an autosave, and a record full of half-made selections is one nobody can read — `confirm_selection` is the commitment and the one that carries a name. No table, column, policy or permission changes and no behaviour changes: each writes one extra row and returns what it returned before. `tests/trail.js` runs all five against a real Postgres, asserts the row each writes, that a refused call writes nothing, and **that every tag written is one `ACTION_LABEL` names**, so a row can never land in the record with no label and no section. |
| Activity record | **A campaign's events are filed under the campaign, not the creator.** The pane reads `activity_log` by subject, and every per-creator event — a step moved, a shoot or publish date changed, a fee corrected, a creator withdrawn or reinstated — was written with the creator's name as the subject, so none of it ever reached the campaign's own Activity pane: it showed the invoice, the lock and the bulk dates and nothing else. `logSubject()` is the campaign's raw title, which is the value the pane queries with, and the creator is named in the detail. `tests/prod.js` opens the pane and asserts the step moves are in it, because nothing opened it before. Table (when, tag, subject, detail, who) grouped by day, filter tabs per section (Clients, Team, Content Review, Creator Campaigns, Short Links, Services), opened from a link in the top bar. |
| Letter lifecycle: void and delete (`js/documents.js`, `js/crm.js`) | **Voiding and deleting are two authorities and two acts.** `letter_set_void(p_doc, p_reason)` requires **Clients: Manage** (`allowed('clients', 'manage')`; it was its own `can_doc_void` switch until 2026-09-22), applies to a **verified** letter only, takes a reason, records `voided_by` and `void_reason`, and reverts **only the service lines that letter alone was holding confirmed** (`letter_sole_services`: a line another verified, unvoided letter also maps stays confirmed). `letter_delete(p_doc, p_confirm, p_reason)` is gated on the portal's existing `can_remove` (an admin's by default), takes the **serial typed back** and a reason, applies the same safe reversal, and leaves a `client_document_deletions` row carrying the former id, the serial, the actor, the time, the reason and the service ids — and **no document content**. There is **no restore and no un-void**: a void is a recorded reversal, not a switch. There is **no storage side and no quarantine**, because the letter PDF is never stored and there is no signed upload: the row is the letter, so the sheet says the deletion is immediate and cannot be undone. Both are sheets, not `confirm()`, because each needs something typed. Both menu items carry `data-need="clients:manage"` and the database checks the live permission again when the button is pressed, so a permission taken away while the sheet is open is a refusal and not a deletion that already happened. |
| Documents Register (`js/letters.js`, `js/register.js`, `js/verify.js`, `verify/`, `supabase/schema.sql` "THE DOCUMENTS REGISTER") | **One engine, four kinds, one letterhead.** `js/documents.js` exports the pen (`pen()`, the primitives and the letterhead the Letter of Offer draws with, plus `embedFonts`, `embedLogo`, `letterDate`), and `js/letters.js` draws the quotation cover, the letters to clients and the HR letters on it, so the two cannot come out on two different sheets of paper. A row in `documents` is the whole snapshot (recipient, body per language, signatory, languages) and the file is redrawn on Download, never stored. The page follows the Word templates: PRIVATE & CONFIDENTIAL, Our Ref and Date, the recipient (To, address, Attn; for HR the name, the position, `{staff code} / {NRIC}` and TO BE OPENED BY ADDRESSEE ONLY), the title in capitals, the salutation, the body (one block per language with a hairline between, Chinese through `ADSPACE_ORG.fontCjk` and refused **by name** when that face cannot be fetched, rather than drawn as boxes), the closing, ADSPACE PLT, and for a signed kind a signing space over the signatory's NAME and designation. Every page foot carries the reference, the page count and `This is a computer-generated document. [No signature required. ]Verify document authenticity: go.adspace.me/verify`. `ADSPACE_ORG.fontMed` (`/css/SlateMedium.TTF`, uploaded by the user on 2026-09-18) heads the letters and falls back to Slate Regular wherever the file cannot be fetched. **The serial is the database's to make, per family**: a quotation cover takes the accounting portal's typed (`AQT2607003`); a client letter is `AD/[SA/]{client_code}/{code}` (SA where the client has ever been Active, Paused or Past, or holds a confirmed line); an HR letter is `ADHR/{staff_code}/{code}{YYMM}`; a built one takes `-2`, `-3` where the same base is spent, a typed one is accepted for any family, and `serial_taken()` spans `documents`, `client_documents` and `document_deletions`, so a serial is never handed out twice. `doc_types` seeds once (quotation cover EN/ZH/MS unsigned; Thank-you letter SC; Letter to client GL; Confirmation of employment E; HR letter GL) and is the team's to edit. **HR is its own section of the ladder** (`hr`, beside `register`): `register_may(family, level)` answers HR to `hr` alone and client documents to `register` or `clients`, `documents_read` is that predicate, the Register nav shows for either `view`, and `issue_document` logs an HR letter under the subject `HR` with the kind alone, because the Activity record is read by more people than HR is. **The Register** (`?s=register`) is one register banded Quotation covers / Client letters / HR letters / Other documents, rows Document · Recipient · Issued · ⋯ (`.reg-row`), search on reference, kind and name, a kind filter, `Add entry` (a serial made elsewhere: `register_add`) and `Issue document`, which opens `#docSheet`: the kind decides the fields (client or colleague, To/Address/Attn or Position/NRIC, the language ticks for a cover, the signatory for a signed kind), the type seeds the title, salutation and body with `{first name}` and `{role}` filled, a body somebody has edited is never overwritten by a later select, and the signatory is the signed-in person's name and `team_members.designation`. The client record's Documents pane carries **Issue document** (client kinds only, the client fixed, the main contact on Attn) and lists the client's register rows under its Letters of Offer. Valid is the ordinary case, so there is no state column: `Void` is a chip beside the reference and the row fades; Void and Delete permanently are sheets (`#rvoidSheet`, `#rdelSheet`, the serial typed back), behind `hr:manage` / `register:manage` (`clients:manage` on the record) and re-checked by `document_set_void` / `document_delete`. `team_members.staff_code` (`^[A-Z0-9]{3,8}$`, unique) and `designation` are typed on the Team page and read on the row. **`/verify/`** is the public page every foot names: one field, `verify_serial()` granted to anon, answering an exact reference with the kind, the date and Valid / Void / Replaced, never the recipient, an HR serial as "HR letter" and a Letter of Offer number too; `?s=` prefills. English and Chinese. The root `verify.html` (the Jotform page) is untouched; the `go.adspace.me/verify` link is repointed by the user. `tests/docs.js` drives all of it and reads the PDFs back with pdf.js; `tests/sql.js` section 20 runs the functions against Postgres. Storage on S3 with expiring public links is batch 2. |
| Letter: the signatory (`issuer_name_ok`) | **A letter is signed by a person, never by a permission.** `issued_by` is `team_members.name`, and a production row named `Superadmin` printed that word under ADSPACE PLT on a client's letterhead. `issue_letter` refuses when the name is blank, contains `@`, or is one of the reserved role words (superadmin, admin, administrator, team member, marketing, sales, account, user, root, owner, staff, system, support, test), and the console says to set a real name on the Team page. No designation field: the name alone signs, under `For and on behalf of ADSPACE PLT`. |
| Documents (`js/documents.js`) | The Letter of Offer (kind `offer`; earlier rows `intent`, `cover`), issued by sales to the client from the record, numbered `AQT/INT/YYMMXXX` (sequence per month, file name swaps `/` for `-`), drawn in the browser with pdf-lib on the ADspace letterhead (the reference is the End Thank You Letter): wordmark in Optima, Co. Reg. and address left, the monogram and phone, email, website right; PRIVATE & CONFIDENTIAL; Our Ref / Date (12th September 2026) / To (registered name in capitals) / Attn (billing contact, role); the subject LETTER OF OFFER; Dear {contact}; an opening paragraph; the **To quote lines only** (enquired lines are not priced yet, confirmed lines are past this step) as Description / Rate / Amount, each line carrying its inclusions from `detail` then its pricing basis, period and note, with `per month` mute under the Amount; priced by the month when every line shares a term, so Subtotal, SST 8% and the bold **Payable monthly** are all one month's invoice, and the whole commitment is disclosed as a sentence in the TERMS block naming the term and the figure (a one off or mixed letter reads Total instead and carries no term sentence); the TERMS block holds that, the billing basis and the validity (30 days); `priceOf()` computes the stored snapshot and the drawing alike, and the stored `subtotal`/`tax`/`total` remain the whole commitment for the record and the pipeline; Yours sincerely, ADSPACE PLT, the signed-in person's team name; an acceptance block the client signs (Signature and company stamp, Name, Designation, Date), kept on one page with the closing and with the sentence naming the reference, the date, the page count and the figure being accepted; the reference in every page foot and an initials line on every page but the signed one, so a first page cannot be swapped after execution; the monogram bottom centre and Page x of y on every page. Once the client signs, the formal quotation and invoice are issued outside the portal. Stored as issued in `client_documents`, Void then Delete. |
| Client portal (`client/`, `js/portal.js`) | One sign-in by email link (`signInWithOtp`, redirect `/client/`; not BRN and name, no typed code), one client on the page. Access is one switch on a contact (`client_contacts.portal_access`), set from the contact's ⋯ as **Enable portal access** / **Revoke portal access** with Undo, and marked on the row by a green `Portal access` chip because a sign-in is a live state. Enabling opens a sheet naming the person and the sign-in address with **Send invitation email** **unticked**: access is the decision, an email is a separate one, and the client can sign in without ever getting one. **Send invitation** stays in the ⋯ (`invite-member`, `notify: true`) for when they ask. Undo on a revoke emails nobody. **Access alone is enough to sign in**: the `portal-login` edge function (Verify JWT off, unauthenticated by design) makes the login on the way in, for an address that is a live contact with `portal_access` and for no other, so nothing depends on a call the console made days ago. Sign-ups stay closed on the project, which is what stops the page minting accounts; `signInWithOtp` carries `shouldCreateUser: false` and `portal-login` is what puts the account there first. **One address can hold access at several clients** (`portal_clients()` returns every one), so a group of companies is one login with a company select on the page. The page reads and writes only through `get_portal`, `portal_request` and `portal_withdraw` (security definer, keyed on the signed-in email); a client account never touches a table. Sections in order: Overview (registered name, registration no., billing address, market, account manager, status chip; contacts read-only; one action Request change), Services (confirmed and quoted lines with chips and totals; enquired lines never shown; Upgrade / Downgrade / Cancel in the ⋯ of a confirmed line), Requests (appears once one exists; Withdraw while Requested, with Undo), Letters (Download redraws the snapshot), Engagements (Content Review and each campaign opened by their own token links; hidden when there is nothing), Payment (only once `ADSPACE_ORG.bank` is set), **Portal access** (Person · Sign-in email: the client's own people who hold a sign-in, named for that rather than "Account", which read as the viewer's own settings). English and Chinese. Covers: Client sign-in / Check your email / Access denied / Unable to load. |
| Console: Requests (`js/crm.js`) | On the client record after Documents, shown once a contact has portal access or a request exists: Request · Fee · State · ⋯; states Requested → Reviewing → Approved or Declined → Applied (select, tinted; Withdrawn as a chip); Reply in the ⋯ sets a fee and a reply the client reads; every change logged with who decided. Approval never changes a service line; a person applies it. |
| Team-only policies (`is_team()`) | Clients have logins too, so `authenticated` no longer means the team: `team_members`, `team_roles`, `services`, `activity_viewers` reads, `activity_log` writes, the `content` storage bucket, `delete_client` and both edge functions (`sign-upload`, `invite-member`) check an active team row. The team list is never derived from `auth.users`: the cutover sweep runs only while `team_members` is empty and skips any address recorded as a client contact, and the repair at the foot of the schema stands down a contact the old sweep let through. **One address is never both**: `no_team_client_overlap` refuses portal access to an active team address and refuses the team to a live portal contact (on insert, and on the update that would move a row into the overlap, so a legacy row stays editable), and `portal_clients()` returns nothing for a team address, so even a legacy overlap cannot open both. |
| Money (`js/money.js`) | The only formatter, and the only place a price is adjusted. RM for MY, S$ for SG; SST 8% unless `sst_applies` is false; two decimals on every total. `TERMS` holds the term factors (3 months `1/0.9`, 6 the baseline, 12 `×0.95`, 24 `×0.9`); `rateFor()` gives the rate a line is billed at, rounded to the cent where it is charged, and `termWord()` names the adjustment. The console, the letter and the client portal all read it, so they cannot drift. |
| State in the URL | `?s=clients|review|campaigns|links|services|team`, `client=`, `campaign=`, `tab=`, `set=`, `new=`. A refresh (or switching tabs) lands where the person was with what they typed. Kept as `?s=` after weighing `/admin/crm` style paths; true path routing (`/admin/clients/hkl-lim-team`) needs the root `404.html`, which is the user's site and never edited, so it is not available on GitHub Pages. `client=` carries the client's readable `slug` (set once from the name, never following a rename, a clash numbered), and `clientByKey()` resolves a slug or an older UUID by the key's shape. The slug is in the console only: it is an address, never access. |

### Non-negotiable validation before "done"

**The behavioural suites are no longer in this repository.** It is public, and
the harness, the database schema and these working notes were all reachable at
digital.adspace.me and readable on github.com; the suites were taken out on the
user's instruction on 2026-09-20. `_config.yml` stops Pages publishing the
rest. **They are in the history and come back with one command**, which is the
first thing to do before changing behaviour:

```
git checkout 02de7c9 -- tests        # then delete the folder again before pushing
```

Everything below still applies. What changed is where the suites live, not
whether they are run: a change to behaviour is restored, run, and the folder
removed again in the same push, because a portal this size cannot be checked by
reading it.

1. `node --check` on every changed `js/*.js`.
2. Local server: `setsid nohup npx --yes http-server -p 8899 -s . >/dev/null 2>&1 &` from the repo root (it dies between turns; restart on `ERR_CONNECTION_REFUSED`).
3. The full behavioural sweep, every suite green. **Read the exit code, never a count of `FAIL` lines**: a suite that throws — a locator that times out because the control it names has been removed, a native time field filled with `2pm` — prints no `FAIL` line at all, so `grep -c '^FAIL'` reports zero and the sweep reads a crash as a pass. That is how `tests/crm.js` sat broken on `main`, and how `tests/prod.js` sat dead from its bulk-logistics section onward.
   `for s in run camp client cprod bar newbadge prod qr regress backup keyin state race chrome crm sgd team portal creator canvas register geom letter links; do out=$(node tests/$s.js tests 2>&1); rc=$?; if [ $rc -ne 0 ]; then echo "$s: CRASHED rc=$rc"; else echo "$s: $(echo "$out" | tail -1)"; fi; done`
3a. **The wider matrix and the keyboard: `node tests/matrix.js tests` → `matrix: ok`.** A representative screen from every route at **320, 375, 390, 768, 1024, 1280 and 1440**, and again at 1280 and 1440 under **200% browser zoom** — the CSS viewport halved, not the `zoom` property, because Ctrl + is what a reader presses and it fires the media queries. It runs `uxaudit`'s own `inPage()` checks, read out of that file rather than copied, so a rule added there is a rule the matrix enforces, and drives the keyboard through every control that opens a dialog.
3b. **For any change to `supabase/schema.sql`, including a seed or a function: `node tests/sql.js tests` → `sql: ok`.** It starts a throwaway Postgres 16 cluster and runs the real thing: the stage clock, the team sweep and repair, the overlap guard, the rate card seed, the whole creator delivery path, the client's recorded decision, the creator rating and the handle reader. Every other suite drives a browser against `stub2.js`, so PL/pgSQL is the one part of the portal nothing else checks. It builds its own cut-down tables, so a column added to the schema has to be added to that fixture too. It prints `SKIP` loudly where Postgres is missing rather than passing quietly.
4. `node tests/uxaudit.js tests` → must print `uxaudit: ok`. It walks every page and state at 1280 and at 390 with a coarse pointer, and the console again in dark at both widths, and fails on: sideways overflow, a cell alone on its row, uneven card padding, a header cell off its column (`cols`), the same cell starting at different x on different rows (`column`), a last column that ends short of the row's right edge on a phone (`edge`), uneven gaps between the blocks of a section (`stack`), text under 11px (`type`), a value that wraps or clips, buttons in one row at different heights (or widths on a phone), a control under 38px (44px touch; `.btn-sm`, `.input-sm`, `.select-sm` may be 32px on desktop), a field or icon-only button without a name, more than two filled actions in a view, text under 4.5:1, a control border under 3:1, a control that takes focus without a ring. The review page's post mockups (`.card-stage`) are outside the audit.
5. Screenshots at 1280 and 390 of every screen the change touches, in both themes when the change is visual (`SHOTS=1 node tests/uxaudit.js tests` for the whole walk into `tests/walk/`). Each one is opened and read against the phone checklist in `DESIGN.md`, not just taken.
6. For any PDF change: `node tests/pdfreal.js tests` and `node tests/pdfcases.js tests` → `pdfcases: ok`, which draws the letter against the extremes one fixture never reaches and asserts what only geometry can say — the four AcroForm fields exist as widgets with valid rectangles, the execution block is all on one page, and nothing drawn crosses the margins, read back from glyph positions with pdf.js because a clipped string decodes perfectly as text. `node tests/pdfshot.js tests <file.pdf>` rasterises a letter. Both need `npm i pdfjs-dist@3.11.174 --prefix tests/pdfx` and the local server.
7. Version stamp: every stylesheet and script tag carries `?v=YYYYMMDD` (`20260920a`, `b`… for further pushes the same day). Bump on every push that changes CSS or JS with one `sed` over every HTML file that carries one.
8. The deploy is confirmed from the GitHub Pages workflow run for the merge commit (`pages build and deployment`, conclusion success). `curl` to digital.adspace.me returns 403 from the sandbox and proves nothing.


## 4. Workflow and constraints

### Git and delivery
- Branch `cl/exciting-mayer-fvg0dc`; one PR per batch, squash-merged to `main` by Claude; after a merge the branch is reset onto `origin/main` (`git checkout -B … origin/main`, force-with-lease push). Never stack on merged history.
- Commit message: a one-line title in plain English about what the person gains, a body in the same register, then the two trailers from the session reminder. PR body: What changed, Test plan (ticked), the two footer lines. No model identifiers in any repo artifact.
- The user is in "proceed and merge" mode: build, test, push, open the PR, merge, confirm the Pages build, then report. Ask only when readings differ materially; never re-explain settled decisions.
- When a schema change ships, the report says "re-run `supabase/schema.sql`" in the user-side list. Edge functions (`sign-upload`, `invite-member`, Verify JWT off) are deployed by the user with the Supabase CLI.
- Never ask for a URL, key or asset the repo or config already holds (`brandLogo`, favicon, fonts in `css/`). Check `js/config.js` and `css/` first.

### Folder and file rules
- **`index.html` is the portal's front door and is now ours**: a cover carrying the mark and `Digital Portal` and nothing else, built from `css/portal.css` so it shares the portal's ground, ink and display face, and needing no script. No sign in and no menu of the rooms inside: every door into this portal is a link somebody was sent (the console behind Supabase Auth, the client pages behind their own tokens), so a list of them would be an index of what exists and a set of dead ends for anybody without a link. The user asked for it on 2026-09-20; the rule below still holds for every other root file.
- Root HTML files (`404.html`, `verify.html`, `ap01.html`, `ap02.html`, `ap03.html`, `ap-dale.html`, `accv-new.html`, `3pform.html`, `einvoiceinfo.html`, `interview-quiz.html`, `sales-program.html`, `supplier.html`) are the user's existing site. Never edit them, never edit `404.html`.
- Portal pages are folders with an `index.html` and clean paths (`/admin/`, `/creators/`, `/review/`), never `.html` in a URL. New pages start from `docs/PAGE-TEMPLATE.html`.
- One stylesheet `css/portal.css`; one script per section in `js/`; `js/api.js` is the only Supabase client; `js/money.js` the only money formatter; `js/chrome.js` the only header and footer; `js/menu.js` the only copy of where a row ⋯ opens and what closes it; `js/copy.js` the only copy of what a Copy button says when it has copied (four buttons each said it differently: one swapped its label, one a span inside itself, one wrote into a `.msg` line, one tinted itself); **`js/words.js` the only copy of a word two pages share** (loaded before every page script); **`js/state.js` the only copy of how a list says it is loading, has nothing in it, or could not be read** (three files had written those three states four different ways, and one of the four reported a failed read as an empty list). No build step, no framework, no bundler, ES5-style function scripts wrapped in an IIFE.
- **Server-side code that is not a Supabase edge function lives in `workers/`**, one folder per Worker, with its source and a README naming the account, the zone, the Worker, the custom domain and the three variables. `workers/links/` is the `hi.adspace.me` redirector. It is deployed from the repository and is not part of the Pages build.
- Docs for setup live in `docs/` (`CONTENT-REVIEW-SETUP.md`, `S3-UPLOAD-SETUP.md`, `DRIVE-IMPORT-CHECK.md`, `FIX-ACCESS-DENIED.md`, `CREATOR-UPLOAD-CHECK.md`).
- **A change to one database function ships as a file in `supabase/migrations/`, not as a re-run of the whole schema.** `supabase/schema.sql` carries fifteen top-level data migrations (team roles and their defaults, the rate card minimums and inclusions, client slugs, the stage clock backfill, the team stand-down repair, the Marketing and rednote renames) and drops and recreates fifty-six policies, triggers and indexes. Every one of those is idempotent and several are guarded, but re-running all of it on a live database to replace one function body is unnecessary exposure. A migration file is dated, narrowly scoped, safe to run twice, carries its own rollback, and is applied standalone by `tests/sql.js`.
- Secrets never enter the repo. The Supabase anon key and the Google browser key are public by design and held back by their restrictions (Google key: websites `digital.adspace.me/*`, Drive API only). The delete code lives in the database.

### Infrastructure and assets (established, do not re-ask)
- Hosting: GitHub Pages, custom domain digital.adspace.me. Cloudflare Pages (`adspace-form.pages.dev`) only builds PR previews; the move to Cloudflare Pages was tried and abandoned (Access Denied), the site stays on GitHub Pages.
- Supabase project `hwwuigvdfubuymchsvyx`; tables and policies in `supabase/schema.sql`; `activity_viewers` is the older per-email list, superseded by user groups.
- Media: S3 bucket `myadspace`, region `ap-southeast-5`, prefix `content`, ARN `arn:aws:s3:::myadspace`, public read through CloudFront at https://mycdn.adspace.me; uploads by signed PUT from the `sign-upload` edge function; Supabase storage capped at 50 MB, so video goes to S3.
- The PDF is never stored: the portal keeps the data snapshot in `client_documents` (client, contact, lines, totals) and redraws the file on Download. Void keeps the row; Delete (only after Void) removes it and the letter can no longer be regenerated. Nothing goes to S3 or the CDN unless a person uploads it (the campaign invoice PDF is the one upload).
- Brand assets: header mark https://mycdn.adspace.me/adspace-brandname.png (`brandLogo`), favicon and og:image https://mycdn.adspace.me/adspace-favicon.png; in the repo for print: `css/adspace-mark.png` (the monogram, with alpha, taken from the reference letter), `css/SlateBook.TTF` (text), `css/SlateRg.TTF` (heavier face), `css/OPTIMA.TTF` (the wordmark). Older `ADspace.png` and the website favicon were rejected.
- Issuer (`ADSPACE_ORG` in `js/config.js`, entered by the user): ADSPACE PLT, registration 202304002162, SST J31-2401-32100002, 61-02, Jalan Mutiara Emas 2A, Taman Mount Austin, 81100 Johor Bahru, Johor, Malaysia, advertise@adspacestudios.com, 60187625233 (printed as (60)18 762 5233), adspacestudios.com. Bank line still blank.
- Emails: support and issuer advertise@adspacestudios.com, account manager marketing@adspacestudios.com, admin login adspacestudios@gmail.com. Terms https://adspacestudios.com/legal/policies.
- Short links: new links are written under `ADSPACE_CONFIG.linkHost` (`hi.adspace.me`); `go.adspace.me` stays alive for the Rebrandly QR codes already printed on slides, which encode the whole address. About 50 slugs on Rebrandly. No redirector is built for either host yet.
- Google Drive import uses the browser key in `js/config.js`; the Drive folder link is the only URL accepted (`/folders`).

### Behavioural guardrails the user gave (verbatim spirit)
- "This should be automatic on your structure when designing." Every rule in `DESIGN.md` is applied before the user sees a screenshot, never after.
- "Do not state the obvious." "Be concise and direct." No explanatory copy anywhere.
- "Return means go back." Back for navigation, Revert for a state, Restore for a record, Reinstate for a person.
- "Every action needs a way back." Nothing is deleted by one click; soft remove, Undo, Void then Delete.
- "Big card for short messages": a message is a line, not a card.
- "Same status throughout": the console and the client page use one vocabulary and one colour set.
- "Prefilled, not fixed": catalogue values seed a line and stay editable.
- "A phone screenshot is reviewed, not just taken."
- "Why do I need to fill in the handles here": intake asks for what is known on day one only.
- "Not AI SaaS": flat, quiet, corporate, Apple and Cloudflare register.
- "Memory like a goldfish": when a rule is learned it is written here the same push, with the section it belongs to, so it never has to be repeated.
- **Write to the user in corporate register: short, concise, straightforward, instructions first.** This governs the replies in the chat, not only the copy on the screen. No literary sentences, no narration of the reasoning, no essay where a list will do: what changed, what it means for them, what they have to do. Asked for several times before it was written down, which is the failure this line exists to stop.
